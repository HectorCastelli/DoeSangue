#include <stdio.h>

//TMAX = Total campaign time in seconds
#define TMAX 3600

//Fila, Cadastro , Pré-triagem, Triagem, Doação, Lanche

typedef struct person {
  int id;
  int time;
}

int main()
{
  struct person
  int t;
  for (t=0;t<TMAX;t++) { //for each second in functions
    //definir fila inicial
    //Escanear processo (13 posições)
      //Se fila = cheia
        //Checar se pode prosseguir
          //Se prosseguiu começa a contar tempo.
            //Cada vez que ele passa por um processo push pro processo seguinte
  }
    return 0;
}
